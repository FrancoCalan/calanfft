#include "calanfft3.h"
/*
static void init_twiddles_rom(wtype wr_arr[STAGES-1][SIZE2],
				              wtype wi_arr[STAGES-1][SIZE2]) {
    for(int i=0; i<STAGES-1; i++) {
		for(int j=0; j<SIZE2; j++) {
    		wr_arr[i][j] = (wtype) cos(-1*M_PI*j/(SIZE2));
    		wi_arr[i][j] = (wtype) sin(-1*M_PI*j/(SIZE2));
    	}
    }
}
*/
